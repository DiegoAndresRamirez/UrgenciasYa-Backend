package com.urgenciasYa.common.utils.enums;

// Enum to represent the different statuses of a shift
public enum StatusShift {
    PENDING, // Indicates that the shift is pending
    COMPLETED, // Indicates that the shift has been completed
    CANCELLED // Indicates that the shift has been cancelled
}
